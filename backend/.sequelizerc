const path = require('path');

module.exports = {
  config: path.join('database', 'config', 'config.js'),
  'migrations-path': path.join('database', 'migrations'),
  'seeders-path': path.join('database', 'seeders'),
  'models-path': path.join('src', 'models'),
};
